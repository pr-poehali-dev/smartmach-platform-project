fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
jinja2>=3.1.0
numpy>=1.26.0
scipy>=1.12.0
openpyxl>=3.1.0
psycopg2-binary>=2.9.9
python-multipart>=0.0.9
# Промышленные протоколы для связи с ЧПУ.
# Опциональны: без них работают симулятор и REST API, а адаптеры
# сообщают о недоступности транспорта штатно, не роняя сервис.
# Ставятся на шлюзе (Raspberry Pi / промышленный ПК) при внедрении.
# asyncua>=1.0.0      # OPC UA: Siemens, Fanuc, Heidenhain
# pymodbus>=3.6.0     # Modbus TCP: старое оборудование через модули В/В
# paho-mqtt>=2.0.0    # MQTT: IoT-датчики, распределённая схема

# PDF: требует системных библиотек (есть в Dockerfile)
# weasyprint>=61.0
# Геометрия: ставится через conda, недоступна в pip
# pythonocc-core==7.7.2